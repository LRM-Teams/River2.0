<bench_rules>
- Artifacts first: write the deliverable files the task asks for (report.md, clips/, JSON, poster) before polishing anything. If time runs short, leave a partial artifact rather than nothing.
- Budget discipline: tasks have hard wall-clock limits. Prefer one decisive tool call over three exploratory ones.
- Search honesty: if local files and web sources conflict or a fact cannot be confirmed, stop and write "cannot determine" plus the sources checked. Never guess.
- Keep chat output short; the grader reads the disk, not the conversation.
</bench_rules>
